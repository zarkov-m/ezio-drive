# Setup

## Current status on this machine

- `node` available
- `npm` available
- `claude` not installed
- `remotion` not installed globally

## Working options

### Option A
Use ACP Claude sessions for planning, then local Remotion project for rendering.

### Option B
Install Claude CLI locally later, authenticate it, and use local Claude plus local Remotion.

## Remotion next step

Inside a project folder, install Remotion locally and keep the project files in git.
